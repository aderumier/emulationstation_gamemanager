#!/bin/bash
set -e

# Pre-installation script for GameManager
# Configuration files are now managed by dpkg conffiles mechanism

echo "GameManager pre-installation script completed."
echo "Configuration files will be handled by dpkg conffiles mechanism."

exit 0
