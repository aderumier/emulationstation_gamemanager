# Game Collection Manager - Requirements
# Core web framework
Flask>=2.3.0
Flask-CORS>=4.0.0
Flask-SocketIO>=5.3.0
Flask-Login>=0.6.0
Flask-Compress>=1.13
# Flask-Session>=0.5.0  # Removed - using Flask's built-in session management

# HTTP requests and networking
requests>=2.31.0
httpx[http2]>=0.25.0
aiofiles>=23.2.0

# HTML parsing and processing
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Image processing
Pillow>=10.0.0
# pixelmatch - included locally in pixelmatch/ directory

# Security and authentication
bcrypt>=4.0.0

# Environment management
python-dotenv>=1.0.0

# Image manipulation
Wand>=0.6.0

# PDF processing (installed via Debian package: python3-pymupdf)
# PyMuPDF>=1.23.0

# IGDB API integration (using httpx directly)
# Note: pyrate_limiter is deployed locally in the app directory

# Standard library packages (included with Python 3.8+)
# - os
# - json
# - time
# - xml.etree.ElementTree
# - threading
# - re
# - difflib
# - shutil
# - uuid
# - datetime
# - concurrent.futures

# Development and testing (optional)
# pytest>=7.4.0
# pytest-flask>=1.2.0

# Performance monitoring (optional)
# psutil>=5.9.0

# Note: This application uses Python 3.8+ standard library features
# No additional packages are required beyond Flask, Flask-CORS, Flask-SocketIO, and requests
# 
# Additional system packages required:
# - python3-jellyfish (for Jaro-Winkler similarity calculations)
#   Install with: sudo apt install python3-jellyfish
# 
# Note: pixelmatch is included locally in the pixelmatch/ directory
